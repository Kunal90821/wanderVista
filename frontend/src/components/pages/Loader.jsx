import React from "react";
import "../../styles/loader.css"; // Custom CSS for page flipping

const Loader = () => {
    return  (
    <div className="bg-[#fff0] relative h-[100vh]">
        <div className="loader"></div> 
    </div>
    )
};

export default Loader;
